import 'package:flutter/material.dart';

class PlaylistListSidebar extends StatelessWidget {
  const PlaylistListSidebar({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      color: const Color.fromRGBO(0, 0, 0, 0.08),
      width: 72,
    );
  }
}
